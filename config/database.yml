# PostgreSQL. Versions 9.3 and up are supported.

default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

production:
  <<: *default
  database: chefchapters_development
  username: postgres
  password: <%= ENV["HELLORAILS_DATABASE_PASSWORD"]%>

